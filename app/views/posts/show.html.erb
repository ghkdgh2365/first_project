<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
  <%= tag_icons @post.tag_list %>
</p>

<%= post.title %> <small>( <%= post.comments.size %> )</small>
<%= render "comments/comments" %>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>